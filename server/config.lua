AxIdentity = AxIdentity or {}

AxIdentity.cfg = {
  enable_nui          = true,
  enable_cmd          = true,
  mask_license        = true,
  mask_prefix         = 8,
  auto_open_on_ready  = true,   -- << NEU: beim Charakter-Ready automatisch öffnen
}
